package com.nmsguide.app.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Paleta NMS Guide — Material 3 sobrio ──────────────────────────────
// Colores profesionales, sin neón, inspirados en Material 3 dark.
// Basado en: indigo profundo + teal suave + ámbar tenue.

// ─── Primarios (indigo profundo) ────────────────────────────────────────
val PrimaryIndigo = Color(0xFF6366F1)
val PrimaryContainerIndigo = Color(0xFF2D2B55)
val OnPrimaryIndigo = Color(0xFFFFFFFF)

// ─── Secundarios (teal suave) ───────────────────────────────────────────
val SecondaryTeal = Color(0xFF14B8A6)
val SecondaryContainerTeal = Color(0xFF1A3A3A)
val OnSecondaryTeal = Color(0xFFFFFFFF)

// ─── Terciarios (ámbar tenue) ───────────────────────────────────────────
val TertiaryAmber = Color(0xFFF59E0B)
val TertiaryContainerAmber = Color(0xFF3A2E1A)
val OnTertiaryAmber = Color(0xFFFFFFFF)

// ─── Fondos (dark neutral) ──────────────────────────────────────────────
val AppBackground = Color(0xFF0F0F12)
val AppSurface = Color(0xFF1C1C1E)
val AppSurfaceVariant = Color(0xFF27272A)
val AppSurfaceElevated = Color(0xFF2C2C2E)
val AppSurfaceContainer = Color(0xFF222225)

// ─── Texto ──────────────────────────────────────────────────────────────
val AppTextPrimary = Color(0xFFE4E4E7)
val AppTextSecondary = Color(0xFFA1A1AA)
val AppTextMuted = Color(0xFF71717A)

// ─── Errores / estados ──────────────────────────────────────────────────
val AppError = Color(0xFFEF4444)
val AppSuccess = Color(0xFF22C55E)
val AppWarning = Color(0xFFF59E0B)
val AppInfo = Color(0xFF3B82F6)

// ─── Grises neutrales ───────────────────────────────────────────────────
val Neutral50 = Color(0xFFFAFAFA)
val Neutral100 = Color(0xFFF4F4F5)
val Neutral200 = Color(0xFFE4E4E7)
val Neutral300 = Color(0xFFD4D4D8)
val Neutral400 = Color(0xFFA1A1AA)
val Neutral500 = Color(0xFF71717A)
val Neutral600 = Color(0xFF52525B)
val Neutral700 = Color(0xFF3F3F46)
val Neutral800 = Color(0xFF27272A)
val Neutral900 = Color(0xFF18181B)

// ─── Dificultades (soft, sin neón) ──────────────────────────────────────
val DiffEasy = Color(0xFF4ADE80)
val DiffMedium = Color(0xFFFBBF24)
val DiffHard = Color(0xFFFB923C)
val DiffExtreme = Color(0xFFF87171)

// ─── Tipos (soft, sin neón) ─────────────────────────────────────────────
val TypeCrafting = Color(0xFF4ADE80)
val TypeCombat = Color(0xFFF87171)
val TypeExploration = Color(0xFFA78BFA)
val TypeTrade = Color(0xFFFBBF24)
val TypeBuilding = Color(0xFFFB923C)

// ─── Velocidades (soft) ─────────────────────────────────────────────────
val SpeedFast = Color(0xFF4ADE80)
val SpeedSlow = Color(0xFFFB923C)
val SpeedMedium = Color(0xFFFBBF24)

// ─── Opacidades reutilizables ───────────────────────────────────────────
// Usar como: PrimaryIndigo.copy(alpha = 0.12f)
// Valores comunes: 0.12f para fondos de contenedor, 0.08f para hover, 0.05f para sutiles
